<table class="table table-striped">
  <thead>
    <tr>
      <th><%= @player.name %>'s statistics</th>
      <th><%= link_to 'Live game', game_path(@player) %><button type="button" class="btn btn-info" disabled="disabled">Live game</button></th>
    </tr>
  </thead>

  <tr>
    <td><strong>Name:</strong></td>
    <td><%= @player.name %></td>
  </tr>

  <tr>
    <td><strong>Region:</strong></td>
    <td><%= @player.region %></td>
  </tr>

  <tr>
    <td><strong>Win Rate:</strong></td>
    <td><%= @player.winrate.round(2) %></td>
  </tr>

  <tr>
    <td><strong>Recent KDA:</strong></td>
    <td><%= @player.kda.round(2) %></td>
  <tr>
    <td><strong>Kills:</strong></td>
    <td><%= @player.kills %></td>
  </tr>
  <tr>
    <td><strong>Deaths:</strong></td>
    <td><%= @player.deaths %></td>
  </tr>
  <tr>
    <td><strong>Assists:</strong></td>
    <td><%= @player.assists %></td>
  </tr>
</table>

<%= link_to 'Update', player_path(@player) %> |
<%= link_to 'Back', players_path %>

<button type="button" class="btn btn-info" disabled="disabled">
  <span class="glyphicon glyphicon-refresh" aria-hidden="true"></span> Update statistics
</button>
